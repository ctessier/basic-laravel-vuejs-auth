# Basic Laravel and Vue.js authentication

Basic Laravel and Vue.js authentication using Laravel Passport.

## Installation

```shell
$ php artisan migrate
$ php artisan passport:install

# Production
$ php artisan passport:keys
```
